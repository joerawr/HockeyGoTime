# OpenAI API Key (required)
OPENAI_API_KEY=sk-...

# SCAHA MCP - Now uses STDIO transport (spawns @joerawr/scaha-mcp via npx)
# No server URL needed - subprocess is spawned automatically

# Chrome Path (optional - for Puppeteer in SCAHA MCP)
# CHROME_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# Vectorize (optional - only needed for RAG agent)
# VECTORIZE_ACCESS_TOKEN=your_vectorize_token
# VECTORIZE_ORG_ID=your_org_id
# VECTORIZE_PIPELINE_ID=your_pipeline_id

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key

# Firecrawl (optional - only needed for web scraper agent)
# FIRECRAWL_API_KEY=your_firecrawl_api_key

# Gemini API key
NEXT_PUBLIC_LOCKED_MODEL=gemini-2.5-flash
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key

# Google Maps API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Upstash Redis Configuration (required for analytics system)
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token

# Resend (for user feedback emails - https://resend.com)
RESEND_API_KEY=re_xxx
FEEDBACK_EMAIL=your-email@example.com
